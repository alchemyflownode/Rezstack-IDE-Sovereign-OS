'use client';import React from 'react';interface Props { children: React.ReactNode; }interface State { hasError: boolean; error?: Error; }export class ErrorBoundary extends React.Component<Props, State> {  constructor(props: Props) {    super(props);    this.state = { hasError: false };  }  static getDerivedStateFromError(error: Error) {    return { hasError: true, error };  }  componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {    console.error(' Error caught:', error, errorInfo);  }  render() {    if (this.state.hasError) {      return (        <div className="min-h-screen bg-gray-950 flex items-center justify-center p-4">          <div className="glass-card rounded-xl p-8 border border-red-500/30 max-w-md">            <div className="text-6xl mb-4 text-center"></div>            <h2 className="text-xl font-bold text-red-400 mb-2 text-center">              Something went wrong            </h2>            <p className="text-sm text-gray-400 mb-4 text-center font-mono">              {this.state.error?.message || 'Unknown error'}            </p>            <button              onClick={() => window.location.reload()}              className="w-full px-4 py-2 bg-gradient-to-r from-purple-600 to-cyan-600 rounded-lg"            >              Reload            </button>          </div>        </div>      );    }    return this.props.children;  }}