'use client';import { useEcosystemStatus } from '@/services/useEcosystemStatus';import { Activity, Cpu, Network, Zap, Wifi, WifiOff } from 'lucide-react';export function EcosystemStatus() {  const { status, connectedCount, isLoading } = useEcosystemStatus();  if (isLoading) {    return (      <div className="p-3 bg-[#181818] border border-[#252525] rounded-lg animate-pulse">        <div className="h-4 bg-purple-900/20 rounded w-3/4 mb-2"></div>        <div className="h-3 bg-purple-900/10 rounded w-1/2"></div>      </div>    );  }  return (    <div className="p-3 bg-[#181818] border border-[#252525] rounded-lg">      <div className="flex items-center gap-2 mb-3">        <Activity className="w-3.5 h-3.5 text-purple-400" />        <span className="text-xs font-bold text-purple-400">          ECOSYSTEM ({connectedCount}/5)        </span>        {connectedCount > 0 ? (          <Wifi className="w-3 h-3 text-green-500 ml-auto" />        ) : (          <WifiOff className="w-3 h-3 text-red-500 ml-auto" />        )}      </div>            <div className="space-y-2 text-[10px]">        {Object.entries(status).map(([key, service]) => (          <div key={key} className="flex items-center justify-between">            <div className="flex items-center gap-1.5">              <div className={\w-1.5 h-1.5 rounded-full \\} />              <span className="text-purple-300/70">{service.name}</span>            </div>            <span className={\ont-mono \\}>              {service.status === 'connected' ? 'ONLINE' : 'OFFLINE'}            </span>          </div>        ))}      </div>      {connectedCount === 5 && (        <div className="mt-3 pt-2 border-t border-purple-500/20 text-[8px] text-green-400/70 flex items-center gap-1">          <Zap className="w-2.5 h-2.5" />          Full sovereign mesh active        </div>      )}    </div>  );}